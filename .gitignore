# Секреты
.env

# Caddy runtime (сертификаты, состояние)
caddy/data/
caddy/config/

# Node
node_modules/
src/theme/davidv/assets/build/

# Сборка/логи
*.log
.DS_Store

# Артефакты скриншотов (попадают в медиатеку WP, не в git)
tools/screenshot/out/

# n8n Caddy basic-auth (хэш)
caddy/n8n.caddy
n8n/files/
workflows/**/.gitkeep
